language: java
addons:
  apt:
    packages:
      - openjdk-6-jdk
jdk:
    - openjdk6
# Do not install anything so that we can use our custom script. Instead, just
# return true.
install: true
script: "./travis-build.sh"
